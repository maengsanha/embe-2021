#ifndef _COUNTER
#define _COUNTER

#include "device_status.h"

const unsigned int COUNTER_MODE = 1;

/**
 * counter - handles mode 2(Counter)
 *
 * @status: current status of the device
 */
extern inline void counter(struct device_status *status) {
  
}

#endif