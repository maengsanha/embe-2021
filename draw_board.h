#ifndef _DRAW_BOARD
#define _DRAW_BOARD

#include "device_status.h"

const unsigned int DRAW_BOARD_MODE  = 3;

/**
 * draw_board - handles mode 4(Draw board)
 *
 * @status: current status of the device
 */
extern inline void draw_board(struct device_status *status) {
  
}

#endif